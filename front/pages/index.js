import React from 'react';
import AppLayout from '../components/AppLayout'

const Home = () => {
    return (
        // 웹레이아웃으로 감싸준다
        <AppLayout>
            <div>Hello, next</div>
        </AppLayout>
    );
}

export default Home;